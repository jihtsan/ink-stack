# PaperCraft 工作台 UI 验收

日期：2026-09-05。目标为在墨栈现有四组件及真实服务契约上适配所选 Stitch 设计。

## 视觉证据

- 源文件：`assets/stitch/14793854115977674732/1d754e6f1bfe47b189d2dd5f2150286d/code.html`。托管截图主体为空，故通过本地 HTTP 在应用内浏览器渲染 HTML 后取图。
- source visual truth path：`.local/ui-qa/reference.png`，1440×1000。
- implementation screenshot path：`.local/ui-qa/desktop-final.png`，1440×1000。
- viewport：1440×1000 CSS px；两份浏览器输出均为1440×1000像素，按相同尺寸直接比较，没有缩放或拼接处理。
- state：桌面浅色工作台，选中一个组件，原始设备规格1072×1448。参考选中日历；实现选中日期，使用真实4×6网格与四个已有组件，而非参考的模拟4×4数据。
- full-view comparison evidence：同一工具输出同时打开参考和最终实现，检查三栏、白色浮动卡片、设备机身、属性栏、选中边框与顶部/底部控制。
- focused region comparison evidence：`.local/ui-qa/reference-library.png` 与 `.local/ui-qa/implementation-library.png` 已同时打开，检查分类、尺寸筛选、卡片标题、图标和间距；完整视图也用于核对布局比例。
- 窄屏：`.local/ui-qa/mobile.png`，390×844 CSS viewport，浏览器 full-page 输出374×2485（纵向滚动条宽度未包含在内容图中）。未发现页面横向溢出；画布、组件库、属性面板顺序排列。
- 预览/发布状态：`.local/ui-qa/preview-published.png`，1440×1000。

## Findings

当前无待修复的 P0/P1/P2 项。以下为明确的产品适配：

- 保留 InkStack 墨栈品牌和四款真实组件；不展示未实现的照片墙、Google Calendar、贴纸、天气、电池估计及模拟收费配额。
- 采用现有系统字体回退，不从网络加载 Inter 或中文字体。字体栈和层级贴近参考，但不宣称与参考字体逐像素一致。
- 保留实际保存、发布、预览、设备地址功能，使用属性标签切换。中央机身为这些实际操作保留工具栏空间，较参考略小。
- 组件尺寸显示在图层和属性栏，卡片内部留给内容与完成数量。

五项保真检查：

1. 字体：无衬线中文与系统字体回退；标题、说明、输入及设备文字层级清晰，日期副标题不再裁切。
2. 间距：三栏独立面板、组件卡片、黑色机身及底部工具关系与参考一致；适配窗口不扭曲屏幕比例；390px窄屏控件可访问。
3. 色彩：浅灰工作区、白色面板、暖灰设备屏幕、蓝色选中/主操作、语义状态色。没有继承参考中虚构的电量或授权状态。
4. 图像和图标：Google Material Symbols 官方SVG原样本地托管，附Apache 2.0许可；最终预览为真实后端PNG。参考照片属于未实现组件，未以假图片或绘图替代。
5. 文案：中文产品操作对应真实功能。设备画布明确为布局示意；过期预览及下载链接标明旧预览；Codex数值仅由实际后端预览展示。

## 比对历史

1. `.local/ui-qa/desktop-v1.png`：发现设备460px上限留下过多空白、短日期行裁切、待办图标位于文字上方。调整自适应机身宽度、日期网格和图标行内对齐。
2. `.local/ui-qa/desktop-v2.png`：上述视觉问题修复。独立源码复查发现当前预览无法重新生成、PNG模式仍响应布局快捷键及100%比例扣除机身边框。顶部预览改为强制生成；PNG模式忽略布局快捷键；原始比例直接设置屏幕宽度。
3. 1072×1448设备设置时发现顶部尺寸标记裁切，移入组件右上方。最终图 `.local/ui-qa/desktop-final.png` 复查通过。

## 交互与工程证据

独立测试服务：`http://127.0.0.1:3211`，专用数据库 `.local/ui-verification-20260905`；现有3210服务的看板和设备地址未修改。

- 登录；添加四种组件；搜索无结果/清除筛选；数据监控分类仅显示Codex。
- 编辑文字并保存，重载后保留；尺寸调整；位置表单移动；非法碰撞回退；鼠标拖动从第0列吸附到第2列；删除和撤销。
- 图层切换属性；网格开关；100%时实测画布宽600 CSS px，对应600px屏幕；回到适应窗口。
- 实际PNG预览完成；未改变编辑修订时再次点击顶部预览生成不同预览URL；PNG模式按Delete后日期组件仍存在。
- 独立测试草稿保存修订4并发布，设备栏显示已发布修订4；随后在窄屏修改测试屏幕为1072×1448、保存修订5并更新预览。
- 390px下属性面板可编辑屏幕宽高和名称，页面无横向溢出。
- 最终浏览器控制台error记录为空。
- Node24：`npm run typecheck`、`npm run lint`、`npm test`通过，13文件/72测试；完整生产构建通过，最终CSS修改后网页构建再次通过。
- 现有局域网3210服务首页HTTP200，引用最终构建资源`index-DdzoV5aw.js`，无需重启后端。

## Follow-up Polish / 验证边界

- 本轮未新增日历、天气、照片或贴纸后端能力；真实Codex账号登录、Kindle刷新与电池续航不作为此次UI验收内容。
- UI布局示意与后端PNG的字体排版不要求一致；使用PNG查看最终设备画面。
- 无新增npm依赖；未做新的设备发布或地址轮换。

final result: passed

## 2026-09-05 组件优化复验

基于同一参考 `.local/ui-qa/reference.png`，进一步优化日期、待办、文字、Codex 四种组件的浏览器示意及服务端渲染。没有新增依赖或改变配置/数据加载契约。

- 桌面最终图：`.local/component-qa/editor-final.png`；实际服务端 PNG 视图：`.local/component-qa/png-final.png`。均在1440×1000 CSS viewport检查。卡片圆角、标题与正文层级、待办完成数及底栏与参考协调；当前真实PNG中的Codex区域与编辑画布裁切一致。
- 紧凑文字：`.local/component-qa/compact-text-final.png`。2×1、大字号、右对齐、隐藏边框和背景，长正文保留完整一行及省略号，无半行裁切。编辑后的过期Codex图片立即撤下。
- 手机最终图：`.local/component-qa/mobile-final.png`，390×844 CSS viewport；DOM宽375px（不含滚动条），无横向溢出。另验证2×1数字日期和星期显示。
- 渲染状态画廊：`.local/component-qa/renderer-gallery.png`，已检查三种日期、小尺寸、长中文、待办隐藏项、Codex双窗口、未知/过期/不可用状态。画廊的额度数字为明确标注的测试样例；工作台截图使用独立测试服务实际采集结果。
- 实际交互：勾选待办后完成数从1/3变为2/3；最大显示为1时，未完成优先展示“检查预览 PNG”，手动排序展示“写一点墨栈”，均提示“还有2项”。文字字号、对齐及边框/背景即时生效。
- 回归先失败再修复：新增5项网页组件测试；组件渲染测试扩充到16项。Node24运行 `npm run typecheck`、`npm run lint`、`npm test`（14文件、89项）及完整 `npm run build` 均通过。最后的文字省略号CSS调整后再次网页构建通过，浏览器检查通过，error日志为空。
- 视觉迭代：第一轮修正紧凑文字半行裁切；第二轮改为标准单行省略号；第三轮桌面、手机及真实PNG检查通过。记录位于 `.omx/state/component-polish/ralph-progress.json`。

仅重启独立验证服务3211以载入新版PNG渲染器，未重启现有3210服务，未修改或发布现有Kindle看板。现有服务需要正常重启才能载入新的服务端渲染代码。实际Kindle显示及续航仍未验证。编辑示意与PNG字体排版存在明确的产品差异，PNG为设备显示依据。

本轮结果：通过，无待修复的视觉问题。

## 2026-09-05 编辑 / PNG 一致性

后续按用户要求取消独立的HTML组件排版。文字、日期、待办复用PNG调用的纯SVG函数及Noto Sans CJK SC Regular字体；新增安全的`@ink-stack/widgets/render`入口，浏览器不导入服务端注册或数据加载。删除原有日期、文字及待办专用CSS排版。字体来源/OFL许可随网页静态资源分发，无新增依赖。

当前PNG与编辑修订一致时，全部组件使用这张图片对应区域，保持原始像素矩形。选中/拖动仅叠加编辑控件；拖动中的图片来源仍按原位置裁切，落点有效后按新位置即时绘制。修改配置会移除过期图片。画布背景跟随主题，网格不再增加背景色。

验证环境：应用内浏览器，独立3211服务；1440×1000及390×844 viewport。页面正常、无框架错误覆盖、console error/warn为空。三种即时SVG与服务端函数输出完全相等；四种组件PNG来源一致、图片正常加载。小尺寸2×1文字的大字号、右对齐及两行换行正确，PNG生成后仍可通过位置表单移动，再实际鼠标拖动从第2列回到第0列。390px无横向溢出。

截图位于 `C:/Users/15151/.codex/visualizations/2026/09/05/01a07002-f4a5-7212-aab4-c5291b2de3d0/`：`consistency-editor.png`、`consistency-png.png`、`consistency-mobile.png`。桌面编辑图关闭网格，选中框仍可见。已对照实际画面确认内容排版一致。

Node24：`npm run typecheck`、`npm run lint`、`npm test`（14文件、91项）及完整构建通过。组件回归覆盖纯SVG等价、文本转义、全部组件PNG裁切及拖动中原始来源坐标。

边界：即时SVG与服务端栅格化可能有细微抗锯齿差异；生成PNG后使用同一图片。首次加载同款字体需下载约16.4MB字体文件。Codex未生成当前预览时保留明确占位提示，不伪造数据。未发布到Kindle。此节取代前文关于两套组件排版属于产品差异的旧说明。

结果：通过。
