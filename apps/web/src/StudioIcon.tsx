/** Self-hosted, Apache-2.0 Google Material Symbols. */
export function StudioIcon({ name }: { name: string }) {
  return <img className="ui-icon" src={`/icons/${name}.svg`} alt="" aria-hidden="true" width={20} height={20} />;
}
