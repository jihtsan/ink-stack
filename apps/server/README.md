# 后端图片服务

状态：待实现。

负责配置校验与存储、组件数据获取、服务端渲染、PNG 发布以及 HTTP 缓存校验。

首期约束：

- 输出图片尺寸与目标设备一致。
- 仅在新图片生成成功后替换已发布版本；失败时保留最后一张有效图片。
- 提供稳定的 PNG 地址，并按实际图片内容生成 ETag。
- 将配置管理接口与设备读取接口分开设计，明确各自的访问权限。
- 对远程数据请求设置超时；不允许配置读取服务器内部地址或本地文件。

具体路由、存储方案和运行命令在首期实现时确定。
