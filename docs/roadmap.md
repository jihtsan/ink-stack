# 开发路线

产品范围见 [主规划](design-v1.md)，技术依赖、执行顺序和验收见 [技术架构](architecture.md)。本文件记录完成状态。

## 0. 初始化与规划

- [x] 初始化仓库、名称与定位。
- [x] 整理网格布局和一组件一目录的设计文档。
- [x] 创建组件目录及规划说明。
- [x] 整理技术选型、任务执行边界、发布一致性和 Codex 采集方案。
- [x] 补充组件入口、密钥录入、共享连接、测试及版本规则。

## 0.5 技术预研

- [x] 验证 SVG/resvg/sharp 的中文排版、2×4 卡片及无透明灰度 PNG。
- [x] 验证目标登录环境的 Codex App Server 可读取真实额度。
- [x] 验证 Node 24 LTS 及原生依赖在目标主机的安装/加载，锁定兼容版本。
- [x] 验证渲染线程路径、忙时 HTTP 响应和超时恢复；需要时改用子进程。

## 1. 组件与网格基础

- [x] 按预研结果建立 npm workspaces、React/Vite 和 Fastify 可运行脚手架。
- [x] 实现屏幕、网格、组件类型和实例契约。
- [x] 实现元数据注册与后端注册，检查重复类型和版本一致性。
- [x] 实现网格边界、碰撞、尺寸规格、像素换算。
- [x] 实现文字组件和 PNG 输出，验证中文与长文本。

## 2. 可用的网格编辑器

- [x] 实现组件库、配置表单、画布及属性面板。
- [x] 实现 codex-local 连接 schema、测试和版本化复用；仅允许适配器声明的配置。首版无 API key/URL 字段，未来 HTTP 适配器的入口与秘密表单待对应适配器实现。
- [x] 实现并测试服务端凭据工具、脱敏、目标限制和轮换缓存失效；当前 Codex 无需保存密钥，未声明秘密被拒绝。
- [x] 实现吸附移动、支持尺寸调整、复制、删除及会话内撤销。
- [x] 增加日期和手动待办组件。
- [x] 增加 codex-usage 组件的 2×4 布局、数据源配置和模拟数据状态。
- [x] 实现草稿、实际 PNG 预览、发布和持久化。
- [x] 实现 SQLite 迁移、串行渲染队列及图片/数据库发布指针切换。
- [x] 验证预览响应乱序、发布失败、重启和旧任务覆盖问题。

## 3. 设备接入与可靠运行

- [x] 验证同机 Codex App Server 额度读取、认证条件和窗口/分组转换；跨主机采集器明确留到后续。
- [x] 完成 Codex 额度定时采集、缓存、未登录/过期/耗尽等状态，核对真实账号结果。
- [x] 完成稳定 PNG 地址、ETag 及失败保留旧图。
- [x] 完成单管理员会话、独立图片令牌及日志脱敏，验证接口访问边界。
- [x] 配置已发布看板定时更新，明确画面更新时间。
- [ ] 确认目标 Kindle 型号、固件和客户端，完成安装配置说明。
- [ ] 验证多轮唤醒、断网恢复和退出阅读模式。

## 4. 后续组件与扩展

- [ ] 天气：数据适配、缓存和过期状态。
- [ ] 日历：本地月历，然后扩展外部日程源。
- [ ] 图片：本地上传、尺寸限制与适配。
- [ ] 按需求增加设备配置导出、多设备及客户端回报。

每个阶段完成后同步 README 的真实状态、启动命令和验证记录。
