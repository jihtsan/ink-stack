# 编辑与 PNG 一致性

复用现有 text/date/todo 的纯 SVG 绘制函数，浏览器加载服务器同款已授权字体。保留服务端注册与数据加载隔离。当前PNG可用时所有组件直接展示同一图片的对应区域；编辑后即时用共享SVG反馈，数据组件提示生成预览。画布背景随主题，网格与选中框仅作编辑辅助，不加入PNG。

先建立SVG结果等价、配置转义和PNG裁切回归测试，再移除独立的HTML/CSS组件排版。验证类型、边界、完整测试、构建与浏览器编辑/PNG切换、长文字、小尺寸、拖动和窄屏。现有Kindle看板不发布。
