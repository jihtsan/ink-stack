# 首版验收记录

日期：2026-09-05。平台：Windows x64，项目独立 Node 24.20.0。本文只记录实际证据；Kindle 已连接并完成识别与文件准备，屏幕操作和长期运行仍待验证。

## 已取得的软件证据

- 官方 Node 发布包 SHA-256 校验通过；npm 安装成功，锁文件已生成；better-sqlite3 原生绑定可加载并执行 SQL。
- 固定 Noto Sans CJK SC 字体（OFL 1.1）生成 `data/probes/chinese.png`，已实际查看中文。输出600×800，8-bit、PNG color type 0、单通道 b-w、无 alpha。
- 四种基础组件每个声明尺寸的绘制样例在 `data/probes`；Codex为2列×4行，另要求最低220×330像素。
- Codex App Server 真实只读接入成功，已与同账号 Codex App 额度显示核对，详见 [真实采集记录](codex-integration.md)。模拟 fixtures 仅用于状态测试。
- Fastify inject 验证管理员鉴权、Origin、schema严格拒绝、布局/修订冲突、两个独立实例、预览与草稿隔离、发布指针、ETag和304、令牌轮换、连接版本与引用保护。
- 故障注入覆盖渲染失败保留旧图、旧发布被新任务取代、损坏文件回退、重启中断任务、定时任务只使用已发布配置。
- AES-256-GCM 工具覆盖密文、归属、keep/replace/clear、缺失/错误主密钥和缓存失效；目标工具覆盖 allowlist、秘密URL拒绝、metadata、IPv4/IPv6绕过及显式私网许可。当前同机适配器不接受任何密钥字段。
- 额度转换覆盖真实窗口/分组、未知字段、耗尽、未登录、API-key-only不支持、过期、超期隐藏、跨重置时间、身份未知/切换不复用旧值。
- 复用工作线程覆盖串行执行、busy拒绝、JS busy-loop中断、真实resvg原生绘制中断后恢复。原生预研使用2048×2048、10000图形有界负载，3秒内回收并成功用新线程生成PNG。本证据不保证所有可能的原生库死锁；遇到不可回收故障时再采用子进程。

## 浏览器联调发现与修复

已通过 Codex 内置浏览器访问真实本地服务并完成管理员登录。发现共享 Ajv schema 的运行时编译与 CSP 冲突，改为构建期 standalone 校验函数；没有扩大页面脚本权限。前端初稿重复的网格/元数据已改为共享包引用。

本地浏览器流程已覆盖：添加组件、选择与配置、拖动画布、保存草稿、预览、发布、创建/轮换图片地址、两个文字实例保持独立配置、组件存在时返回画布设置、移动端窄屏重排。截图保存在 `data/verification`（忽略 Git）：

- `editor-initial.png`：登录后初始三栏编辑器、画布设置、组件库与设备图片区域。
- `two-instances.png`：两个同类型文字组件独立标题/正文配置；此截图早于“适应宽度”文案修正，只作为双实例与布局证据。
- `mobile.png`：窄屏下编辑器重排，无横向溢出。
- `mobile-settings.png`：组件存在时仍可回到画布设置；时区、屏幕尺寸、列数、行数、间距和边距可见。
- `published.png`：从稳定 display URL 读取的已发布 PNG。
- `desktop-final.png`：最终三栏编辑器，额度数值仅展示于服务端 PNG，不画模拟余额条。

隔离实测：草稿修订3、已发布修订2时，预览成功但设备图片SHA-256仍为 `5c241c4cb26f0a5f60f4e1d8009a76df6a45cbb728473f6c5dcbcfe9478cc737`。最终示例草稿/发布均为修订5；进程重启后重新登录，配置和相同图片哈希保留。手机390×844，文档宽375px（另有滚动条），无横向溢出。网格行数由6减到5会拒绝并回退，时区可完整编辑。最后浏览器流程无新增error/warn。

实际PNG已目视检查：中文完整，600×800、PNG IHDR 8-bit / color type 0、单通道灰度、无alpha。图中真实采集快照为 codex 的7天窗口、剩余37%、实际重置时间和09/05 00:55采集时间，不是当前额度保证。布局编辑卡只提示“额度见 PNG 预览”。

最后一轮源码与构建验证已移除假缩放百分比：`apps/web/src/App.tsx` 不再包含 `fitScale`、`适应宽度 <数字>` 或 `缩放 100%` 文案；画布工具显示固定“适应宽度”。

## 最新命令证据

在项目独立 Node `v24.20.0` 下执行：

- `npm ci`：干净安装通过，0漏洞；better-sqlite3固定12.11.1，13.0.3在本机触发node-gyp，详见部署说明。

- `npm run typecheck`：通过；构建 shared/widgets 校验器并完成 shared、widgets、server、web 类型检查。
- `npm test`：通过；最新为 18 个测试文件、175 个测试。
- `npm run lint`：通过；浏览器/服务端导入边界检查通过。
- `npm run build`：通过；生成 shared、widgets、server 与 Vite 前端生产构建。
- `git diff --check`：通过。最后文案和按钮样式修改另通过web TypeScript/Vite构建。
- `node scripts/verify-local.mjs`：2026-09-05 01:57 在 `http://192.168.100.116:3210` 通过；发布图返回 200，`If-None-Match` 返回 304，未授权管理 API 返回 401。修订 6 的 PNG 为 1072×1448、单通道、无 alpha，SHA-256 为 `efe95900ef35e7b8e31c16e9ee1ef51ebd54ca80ff7c8c0d7ed49efb7a654351`。
- `node scripts/probe-codex.mjs --timeout-ms 10000`：通过；最新只读采集 `2026-09-04T17:03:09.231Z` 返回 `status: ok`，默认 `codex` 分组 usedPercent 为 64%，窗口 10080 分钟，重置时间 `1788748098`；另有 `codex_bengalfox` 分组。该命令不创建模型任务、不登录/退出、不消费 reset。

## 视觉依据

界面按本地设计文档的三栏编辑器组织。概念图仅取布局、白底、深色文字和蓝色选择/操作色；其中虚构的额度计费数字、额外组件、实时分钟时钟与图表不属于本版需求，未实现。最终以实际PNG与可用操作验收，而非复制概念图中的错误内容。

检查点包括：组件库/画布/属性三栏关系、中文文字层级、按钮与选择色、设备画布比例和边距、移动端重排、表单及错误反馈。最终截图保存在本机 data/verification（忽略Git）。

实际检查：三栏关系及选择反馈清晰，中文与黑白PNG可读，操作色保持蓝色，4×6网格、24px边距、12px间距与PNG一致，手机表单完整可操作。画布是布局示意，最终图以服务端PNG为准，不要求浏览器字体与resvg排版逐像素相等。

## Goal 完成性复核（2026-09-05 01:08）

本轮读取实际测试断言、执行清单、构建配置和设备说明，未把测试名称或既有勾选单独当作通过证据。上一轮属于实质进展；本轮修复执行清单重复段落，并确认当前服务仍在127.0.0.1:3210监听。

| 原始验收要求 | 证据与结论 |
| --- | --- |
| 安装、严格类型、工作区、构建与启动 | package.json、tsconfig.base.json及上节实际命令输出；软件通过 |
| 四组件、添加/配置/拖动/离散尺寸/删除 | 实际浏览器流程、截图，shared/widgets目录；软件通过 |
| 两个同类型实例独立配置 | two-instances.png及app.test.ts的两个独立文字实例；软件通过 |
| 前后端拒绝重叠、越界、不支持尺寸 | shared网格校验、web/grid.test.ts及服务端API拒绝断言；软件通过 |
| 草稿、预览与发布分离 | app.test.ts比较发布ETag，实际草稿3/发布2哈希不变；软件通过 |
| 中文、指定尺寸、灰度及无透明PNG | 实际PNG目视检查、sharp元数据与IHDR断言；软件通过 |
| 失败、任务乱序、重启保护旧图 | app.test.ts故障注入、序号淘汰、文件损坏恢复、重启中断任务；软件通过 |
| 图片GET不取数/渲染，条件请求304 | app.test.ts计数断言和本机HTTP200/304/401复核；软件通过 |
| 连接版本、秘密边界与测试 | 连接引用隔离测试、credentials/target/connector测试；Codex、QWeather 和 Google 的秘密均留在服务端加密凭据仓库，浏览器只收到脱敏状态 |
| Codex真实只读集成与异常状态 | 真实握手及额度交叉核对；normalize/widget-data/connector断言区分缺失、耗尽、过期、登录失效及账号切换；软件通过 |
| 类型检查、测试、构建、浏览器流程 | 上节既有测试与本轮天气/图片/Google/调度测试，以及隔离浏览器烟测；软件通过 |
| Kindle型号/固件、三轮唤醒、断网恢复、续航 | 已识别 PW3、固件 5.16.2.1.1、1072×1448，并完成设备文件准备；屏幕操作、三轮唤醒、断网和续航仍未通过，不能以HTTP成功替代 |

01:57再次复核实际运行服务：草稿/发布修订均为6，四个组件，PNG为1072×1448单通道无alpha，200/304/401通过。服务从回环地址切到指定私网接口，进程监听 `192.168.100.116:3210`，没有新增防火墙或公网转发。

## 工作流修复证据（2026-09-05）

本轮在独立 worktree、Node 24.20.0、临时数据目录和空闲端口 `127.0.0.1:4173` 上完成验证；没有读取或修改现有 `3210` 服务、主目录数据、凭据或设备配置。

- 天气：应用默认保存 QWeather v1 连接，服务端使用当前/逐日经纬度接口、受限 HTTPS transport、加密凭据、持久化缓存和测试入口。未保存输入的测试不会写入连接、草稿、缓存或发布；QWeather 真实 Host/密钥未配置，因此只记录模拟上游证据。
- 图片：服务端提供相册/管理员目录登记、鉴权上传、真实格式解码、坏图/空目录状态、扫描列表、选择持久化、引用保护和 PNG 接入。浏览器烟测登记并扫描了隔离空相册；未把空扫描误报为有图片。
- Google：应用提供会话绑定 state、固定回调、授权码交换、加密 access/refresh token、刷新、撤销、CalendarList 和 Events 分页读取；单元/集成测试使用模拟 HTTP。Google Cloud 客户端配置和用户授权未完成，不能称为真实 OAuth 通过。
- 预览/发布/设备：浏览器完成登录、添加天气/图片/日历、自动防抖预览、PNG 发布、一键发布、设备地址生成和刷新周期保存；发布成功只表示服务端快照可用，未把它标为 Kindle 屏幕回执。
- 浏览器控制台：第一次调度控件烟测暴露了输入事件读取时序错误，修复后重新构建、重载并完成同流程；修复后的页面未产生新的 error/warn。

代码集成测试与真实供应商/实机证据保持分列：本轮没有向 QWeather 发送真实密钥，没有完成 Google 用户授权，也没有以浏览器服务成功替代 Kindle 连续唤醒、断网恢复和续航验收。

## Kindle 实机准备证据

- Windows 将设备挂载为 Kindle USB 存储；`system/version.txt` 报告固件 5.16.2.1.1，KOReader 日志报告 `KindlePaperWhite3` 与 1072×1448。只记录序列前缀 `G090`。
- 设备已有 KUAL、MRPI、KOReader 和越狱痕迹；接入时没有 `linkss` 和在线屏保扩展。
- MobileRead linkss 归档 MD5 与发布页一致，PW3 安装文件 SHA-256 为 `65a23dd71a2c4f41e4733e3290b38ab1706a265eb14cd170b1ff0ea72148a804`。
- kndl-online-screensaver 使用上游提交 `3356a0d75d9a9094f91156f5e40173516db5cefb`。专用配置关闭日志、不写 FAT 屏保副本、不请求缩放、更新后关闭 Wi-Fi；8 个 shell 脚本全部转换为 LF 并通过 `bash -n`。
- 安装文件与 13 个扩展文件已复制到设备用户存储，源/目标 SHA-256 逐文件一致。用户通过 KUAL/MRPI 执行安装；重连后 `linkss` 版本为 0.25.N-r18981，安装包已被消费，MRPI 日志明确记录 `linkss:install::done` 与 `Success! :)`。

首次实机请求于 02:19:04 到达服务端，用户确认屏幕成功显示 InkStack。实机反馈 1072×1448 下固定像素字号偏小；四个组件随后按相对于 600×800 基准的屏幕密度缩放，PW3 系数约 1.79。72 项测试、类型检查和生产构建通过后发布修订 7；PNG 为 1072×1448、单通道、无 alpha，SHA-256 为 `1473bbbe16d18849246c5824e2b2a0eaddc34ee5d701ce897df7c3e5d6d0b63c`。本机已目视检查放大后的排版，仍待设备下一轮唤醒确认实机可读性。

## 保留验收项与限制

- 已获得 Kindle 型号、固件、USB 文件访问及 linkss 安装成功证据；尚未完成在线画面确认、三轮唤醒/下载/显示、断网恢复、退出恢复阅读和至少一昼夜续航，不能以软件测试替代。
- 只验证当前 Windows x64 主机；未验证 NAS/ARM、容器及公网部署。
- 外部供应商证据：本轮代码接入和模拟上游测试已完成；QWeather 真实 Host/密钥与 Google 用户授权未配置，不能宣称真实供应商联调通过。
- 图片历史暂未自动清理，长期运行需管理磁盘占用。未作长期常驻与功耗承诺。
- 没有推送远程、开放公网、修改防火墙或刷写 Kindle 固件。为实机联调已监听一个指定局域网接口；linkss 通过 MRPI 安装成功。
